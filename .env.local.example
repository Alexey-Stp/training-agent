# Local development environment
# Copy this to .env for local development outside Docker

TELEGRAM_BOT_TOKEN=your_bot_token_here

# Local database (when running postgres in Docker but app locally)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/triathlon_coach

# Local Redis (when running redis in Docker but app locally)
REDIS_HOST=localhost
REDIS_PORT=6379

NODE_ENV=development
LOG_LEVEL=debug
DEFAULT_TIMEZONE=Europe/Prague
